# ADR-004 — 3D/AR Strategy

## Status

Accepted

## Decision

Use `model-viewer` for product 3D/AR in Phase 1.

Use `ModelViewerBoundary` for error/fallback.

## Reason

`model-viewer` is practical for MVP. Building custom WebXR is unnecessary now.
